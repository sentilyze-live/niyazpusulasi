git_url(ENV["MATCH_GIT_URL"] || "https://github.com/sentilyze-live/ios-certificates.git")

storage_mode("git")

git_branch("master")

type("appstore")

app_identifier([
  "com.niyazpusulasi.app",
  "com.niyazpusulasi.app.widget"
])

team_id("C4WUTK99E3")
